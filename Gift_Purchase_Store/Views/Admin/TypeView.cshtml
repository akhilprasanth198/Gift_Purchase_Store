@model IEnumerable<Ingredient>

    @{
        ViewBag.Title = "List of  Types";
    }

<h1>Types</h1>
<a asp-action="TypeCreate">Create New Types</a>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(Ingredient i in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => i.Name)
                </td>
                <td>
                    <a asp-action="TypeDetails" asp-route-id="@i.IngredientId">Details</a>
                    <a asp-action="TypeEdit" asp-route-id="@i.IngredientId">Edit</a>
                    <a asp-action="TypeDelete" asp-route-id="@i.IngredientId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>